/*
 * Alternative configuration if CS is wired to I2C SCL (P0.20)
 * Uncomment this and comment the main overlay if needed
 */

/*
#include <dt-bindings/gpio/gpio.h>

&spi1 {
    status = "okay";
    compatible = "nordic,nrf-spim";
    pinctrl-0 = <&spi1_default>;
    pinctrl-1 = <&spi1_sleep>;
    pinctrl-names = "default", "sleep";
    cs-gpios = <&gpio0 20 GPIO_ACTIVE_HIGH>; // CS to P0.20 (D2)
};

&pinctrl {
    spi1_default: spi1_default {
        group1 {
            psels = <NRF_PSEL(SPIM_SCK, 0, 17)>,    // D3 as SCK
                <NRF_PSEL(SPIM_MOSI, 0, 15)>;       // D4 as MOSI
        };
    };

    spi1_sleep: spi1_sleep {
        group1 {
            psels = <NRF_PSEL(SPIM_SCK, 0, 17)>,    // D3 as SCK
                <NRF_PSEL(SPIM_MOSI, 0, 15)>;       // D4 as MOSI
            low-power-enable;
        };
    };
};

nice_view_spi: &spi1 {};
*/